const cap = (string) => string.charAt(0).toUpperCase()
    + string.slice(1);
const fromCamel = (text) => {
    text = text.replace(/([a-z])([A-Z 0-9])/g, '$1 $2');
    return text.charAt(0).toUpperCase() + text.slice(1);
};
const toCamel = (text) => {
    return text.replace(/(?:^\w|[A-Z]|\b\w|\s+)/g, (match, index) => {
        if (+match === 0)
            return '';
        return index == 0 ? match.toLowerCase() : match.toUpperCase();
    });
};
export { cap, fromCamel, toCamel };
